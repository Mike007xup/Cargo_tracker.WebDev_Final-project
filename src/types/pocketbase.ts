import type { RecordModel } from 'pocketbase';export interface User extends RecordModel {  email: string;  name: string;  role: 'admin' | 'staff' | 'client';  verified: boolean;}export interface Cargo extends RecordModel {  tracking_code: string;  sender_name: string;  sender_phone: string;  receiver_name: string;  receiver_phone: string;  origin: string;  destination: string;  current_status: 'Pending' | 'In Transit' | 'Arrived' | 'Delivered' | 'Delayed';  estimated_delivery_date?: string;  created_by: string;  expand?: {    created_by?: User;  };}export interface CargoStatusLog extends RecordModel {  cargo: string;  status: 'Pending' | 'In Transit' | 'Arrived' | 'Delivered' | 'Delayed';  location?: string;  note?: string;  updated_by: string;  expand?: {    cargo?: Cargo;    updated_by?: User;  };}export type CargoStatus = Cargo['current_status'];export type UserRole = User['role'];